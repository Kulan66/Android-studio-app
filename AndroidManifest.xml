<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.Dogfoodapp"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".UserPageActivity" />
        <activity android:name=".UserProfileManagementActivity" />
        <activity android:name=".DogFoodAndNutritionActivity" />
        <activity android:name=".EducationalContentActivity" />
        <activity android:name=".ShoppingCartActivity" />
        <activity android:name=".UserCartActivity" />

        <activity android:name=".AdminActivity" />
        <activity android:name=".ManageUsersActivity" />
        <activity android:name=".ManageDogFoodActivity" />

    </application>

</manifest>